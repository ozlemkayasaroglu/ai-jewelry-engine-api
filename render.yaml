services:
  - type: web
    name: jewelry-ai-api
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        sync: false
    disk:
      name: uploads
      mountPath: /app/uploads
      sizeGB: 1
